{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "none",
            "request": "launch",
            "name": "PowerShell Launch (Script)",
            "script": "${workspaceRoot}/main.exe",
            "args": [],
            "cwd": "${workspaceRoot}"        
        }
    ]
}